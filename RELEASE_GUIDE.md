["Version bump**: Actualiza `## Version:` en `CodexOfAzeroth.toc` (ej: `0.1.0` → `0.2.0`)\n- [ ] **CHANGELOG.md**: Añade la nueva versión con cambios (formato Keep a Changelog)\n- [ ] **README.md**: Si cambió la compatibilidad de API, actualízalo\n- [ ] **Test en juego**: `/reload` y verifica que `/coa` abre la ventana sin errores\n- [ ] **Commit + Push**:\n  ```bash\n  git add -A\n  git commit -m \"Release v0.2.0: <breve descripción>\"\n  git tag -a \"v0.2.0\" -m \"Release v0.2.0: <detalles>","git push origin main --tags\n  ```\n\n---\n\n## 2️⃣ Generar el ZIP automáticamente\n\n```powershell\n# Opción A: usa la versión del .toc\npowershell -ExecutionPolicy Bypass -File .\build-release.ps1\n\n# Opción B: fuerza una versión concreta\npowershell -ExecutionPolicy Bypass -File .\build-release.ps1 -Version 0.2.0-beta\n```\n\n**Salida esperada:**\n```\n[INFO] Versión detectada: 0.2.0\n[OK]    + CodexOfAzeroth.toc\n[OK]    + Core\n[OK]    + Data\n[OK]    + Locale\n[OK]    + UI\n[OK]    + README.md\n[OK]    + LICENSE\n[OK]    + CHANGELOG.md\n\n============================================\n Release ZIP creado correctamente\n   Ruta : ...\releasesCodexOfAzeroth-v0.2.0.zip\n   Tamaño: 52.3 KB\n============================================\n```\n\nEl script **excluye** automáticamente:\n- `.git/`, `.github/`\n- `.gitignore`, `.gitattributes`\n- `build-release.ps1`\n- Cualquier `releases/` anterior\n\nEl ZIP contiene **solo** lo que el juego necesita:\n```\nCodexOfAzeroth/\n├── CodexOfAzeroth.toc\n├── Core/           (Codex.lua, Database.lua, Search.lua)\n├── Data/           (NPCs, Locations, Factions, Events, Timeline, Concepts, LoreData)\n├── Locale/         (esES.lua — y futuros enUS.lua, frFR.lua...)\n├── UI/             (Categories, CodeEntry, MinimapButton, MainFrame)\n├── README.md\n├── LICENSE\n└── CHANGELOG.md\n```\n\n---\n\n## 3️⃣ Subir a GitHub Releases\n\n1. Ve a: https://github.com/minyorange/codex-of-azeroth/releases/new\n2. **Tag version**: selecciona `v0.2.0` (el tag que subiste con `git push --tags`)\n3. **Release title**: `Codex of Azeroth v0.2.0`\n4. **Description**: copia la sección correspondiente del `CHANGELOG.md`\n5. **Attach binaries**: arrastra `CodexOfAzeroth-v0.2.0.zip`\n6. ✅ **This is a pre-release** solo si es alpha/beta/rc\n7. Click **Publish release**\n\n---\n\n## 4️⃣ Subir a CurseForge\n\n### 4.1 Primera vez (crear el proyecto)\n\n1. Entra en https://www.curseforge.com/wow/addons/create\n2. Rellena:\n   - **Name**: `Codex of Azeroth`\n   - **Slug**: `codex-of-azeroth` (se autogenera)\n   - **Category**: `Lore` / `Library` → elige **Lore**\n   - **License**: `MIT License`\n   - **Description**: pega el contenido de `README.md` (o un resumen)\n   - **Game versions**: selecciona **Retail** → **12.0.0** (o el parche actual)\n   - **Main file**: sube el ZIP `CodexOfAzeroth-v0.1.0.zip`\n3. Click **Submit** → CurseForge revisará (tarda < 24h).\n\n### 4.2 Versiones siguientes (actualizar)\n\n1. Entra en tu proyecto: https://www.curseforge.com/wow/addons/codex-of-azeroth\n2. Pestaña **Files** → **Upload File**\n3. Elige el nuevo `CodexOfAzeroth-v0.2.0.zip`\n4. En **Game Versions** marca `12.0.x` (o el actual)\n5. **Release Type**: `Release` (o `Beta`/`Alpha`)\n6. **Changelog**: pega los cambios de `CHANGELOG.md`\n7. **Save** → de nuevo pasa por revisión.\n\n> **Tip**: CurseForge lee `## Version:` y `## Interface:` del `.toc`. Asegúrate de que coincidan con lo que declaras en la web.\n\n---\n\n## 5️⃣ (Opcional) Automatizar con GitHub Actions\n\nSi quieres que el ZIP se genere solo al hacer push de un tag, crea:\n\n`.github/workflows/release.yml`\n```yaml\nname: Build & Release\non:\n  push:\n    tags:\n      - 'v*.*.*'\n\njobs:\n  build:\n    runs-on: windows-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Build ZIP\n        shell: pwsh\n        run: .\build-release.ps1\n      - name: Upload to GitHub Release\n        uses: softprops/action-gh-release@v2\n        with:\n          files: releases/CodexOfAzeroth-v${{ github.ref_name }}.zip\n          generate_release_notes: true\n        env:\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n```\n\n---\n\n## 6️⃣ Verificar tras subir\n\n| Sitio | Qué comprobar |\n|---|---|\n| **GitHub Release** | ZIP descargable, changelog visible, tag correcto |\n| **CurseForge** | Archivo aparece en \"Files","versión del juego marcada, sin error de validación |\n| **En juego** | Descarga desde la app de CurseForge/Overwolf/WoWUp → `/coa` funciona |\n\n---\n\n## 7️⃣ Referencia rápida de archivos críticos\n\n| Archivo | Qué contiene | Debe estar en ZIP? |\n|---|---|---|\n| `CodexOfAzeroth.toc` | Manifiesto (Interface, Version, SavedVariables, etc.) | ✅ SÍ |\n| `Core/*.lua` | Lógica del addon | ✅ SÍ |\n| `Data/*.lua` | Base de datos de lore | ✅ SÍ |\n| `Locale/*.lua` | Localizaciones (esES, enUS, etc.) | ✅ SÍ |\n| `UI/*.lua` | Interfaz gráfica | ✅ SÍ |\n| `README.md` | Documentación para usuario | ✅ SÍ |\n| `LICENSE` | MIT + aviso de lore Blizzard | ✅ SÍ |\n| `CHANGELOG.md` | Historial de cambios | ✅ SÍ |\n| `build-release.ps1` | Script de build | ❌ NO |\n| `.gitignore`, `.gitattributes` | Config git | ❌ NO |\n| `.github/` | Workflows, issue templates | ❌ NO |\n\n---\n\n## 🎉 ¡Listo!\n\nCon esta guía y el script `build-release.ps1`, cada release es cuestión de:\n\n```bash\n# 1. Bump version en .toc + CHANGELOG\n# 2. Commit + tag + push\ngit add -A && git commit -m \"Release v0.2.0","git tag v0.2.0 && git push origin main --tags\n# 3. Build ZIP\npowershell -ExecutionPolicy Bypass -File .\build-release.ps1\n# 4. Subir a GitHub Releases + CurseForge\n```\n\n---\n\n> **Nota**: El número de `## Interface:` en el `.toc` **debe** coincidir con la build de WoW donde vas a probar. En 2026 con Midnight (12.0.0) usa `120000`. Cuando salga 12.0.2, 12.1, etc., actualízalo **antes** de generar el ZIP final."]